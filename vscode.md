# 查看变量
 VS Code 的「变量」面板对 C/C++ 调试只显示 Locals（局部）和 Registers 两个作用域，全局变量根本不在里面。
1. 鼠标悬停 —— 直接把光标停在代码里的 a 上，会弹出值。对全局变量有效，最快。
2. 监视窗口（WATCH） —— 左侧「监视」面板点 +，输入 a、b、c，之后每次断下都会自动刷新。推荐，一次配置长期有效。
3. 调试控制台 —— 断下后输入 -exec p a，或者直接输 a 也行。
4. 把变量挪进 main() —— 变成局部变量后自动出现在「变量」面板里。刷题时全局变量主要是为了开大数组（放静态区避免爆栈），像 a b c 这种小变量放 main() 里更方便调试。